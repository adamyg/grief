# Generated by makelib.pl, Thu Aug 30 15:46:54 2012
# -*- mode: mak; indent-tabs-mode: t; tab-width: 8 -*-
# $Id: Makefile.in,v 1.3 2013/05/12 21:35:56 cvsuser Exp $
# libiconv/iconv makefile
#
#
#

@SET_MAKE@
ROOT=		@abs_top_builddir@
top_builddir=	@top_builddir@

# File extensions

E=
O=		.o
A=		.a
LP=		lib

CLEAN=		*.pdb *.ilk *.bak *~ *.BAK *.swp *.tmp core *.core a.out

# Compilers, programs

CC=		@CC@
AR=		@AR@
RANLIB=		@RANLIB@
RM=		@RM@
LEX=		@LEX@
YACC=		@YACC@
PERL=		@PERL@
LIBTOOL=	@LIBTOOL@

# Common flags

ARFLAGS=	rcv
XFLAGS=		
COPT=		@CFLAGS@
CWARN=		@CWARN@
CDEBUG=		@CDEBUG@
LDEBUG=		@LDEBUG@
CINCLUDE=	-I. -I.. -I../../libcitrus/src -I../../libcitrus/modules -I$(D_INC) @CINCLUDE@
CEXTRA=		@DEFS@
CFLAGS=		$(CDEBUG) $(COPT) $(CWARN) $(CINCLUDE) $(CEXTRA) $(XFLAGS)
CNFLAGS=	$(CDEBUG) $(CWARN) $(CINCLUDE) $(CEXTRA) $(XFLAGS)
LDFLAGS=	$(LDEBUG) @LDFLAGS@
LDLIBS=		-L$(D_LIB) @LIBS@ @LIBM@ @EXTRALIBS@

RMFLAGS=	-f

# Directories

D_BIN=		$(ROOT)/bin
D_INC=		$(ROOT)/include
D_OBJ=		$(ROOT)/objects/iconv
D_LIB=		$(ROOT)/lib

############################################################

# Targets

VPATH=		. ..

OBJ=		$(D_OBJ)/iconv$(O) $(D_OBJ)/err$(O)
TSK=		$(D_BIN)/iconv$(E)

# Rules

all:			object $(TSK)

$(TSK):			$(OBJ)
		$(CC) $(LDFLAGS) -o $@ $^ -L$(D_LIB) -liconv -lcitrus -lmisc -lw32

.PHONY:		object
object:			$(D_OBJ)/.created

clean:
		-@$(RM) $(RMFLAGS) $(TSK) $(OBJ) $(CLEAN) $(XCLEAN) >nul 2>&1

%/.created:
		-@mkdir $(@D)
		@echo "do not delete, managed directory" > $@

$(D_OBJ)/%$(O):		%.c
		$(CC) $(CFLAGS) -o $@ -c $<

#end
